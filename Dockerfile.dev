# Next.js en modo desarrollo (hot reload). Usado por el servicio "app" del
# docker-compose (profile: app). El codigo se monta como volumen; este
# Dockerfile solo fija la version de Node. Las deps se instalan en runtime.
FROM node:20-alpine
WORKDIR /app
EXPOSE 3000
CMD ["npm", "run", "dev"]
